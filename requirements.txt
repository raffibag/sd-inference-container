# PyTorch 2.4.0 with CUDA 12.4 (pre-installed in AWS SageMaker base image)
# torch==2.4.0
# torchvision==0.17.0

# Core ML libraries - PyTorch 2.4.0 compatible versions
diffusers>=0.27.0
transformers>=4.35.0
accelerate>=0.25.0
peft>=0.7.0
safetensors>=0.3.1

# ControlNet support - PyTorch 2.4.0 compatible
controlnet-aux>=0.0.12
opencv-python

# Image processing
Pillow
numpy>=1.25.2

# Web framework
flask

# AWS integration
boto3

# Memory efficient attention (installed separately due to CUDA dependency)
# xformers (installed via separate pip command with CUDA 11.8 index)